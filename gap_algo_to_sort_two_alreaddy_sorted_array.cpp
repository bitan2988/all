//GAP algorithm to sort two sorted  array without using extraspace
#include<iostream>
#include<bits/stdc++.h>
//#include<array>

using namespace std;

int main()
{
    int a[]={1,3,5,7,9,11};
    int b[]={2,4,6};
    int gap;
    gap = ceil(float((sizeof(a)/sizeof(a[0]))+(sizeof(b)/sizeof(b[0])))/2);
    while(gap>1)
    {
        
    }
    return 0;
    
}